import styled from '@emotion/styled';

export const SButton = styled('button')({
    backgroundColor: '#000',
    padding: '10px 18px',
    borderRadius: '5px',
    border: 'none',
    color: '#fff',
});
